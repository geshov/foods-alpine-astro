<div class="indicator flex-grow flex-shrink">
  <span
    x-text="select.length"
    class="indicator-item badge font-medium -top-1 end-4"></span>
  <button
    @click="view = 'menu'"
    class="btn btn-square btn-outline w-full"
    :class="view ==='menu' && 'btn-active'"
  >
    <box-icon type="solid" name="food-menu" class="fill-current"></box-icon>
  </button>
</div>
