<div class="indicator flex-grow flex-shrink">
  <button @click="$refs.clear.showModal()" class="btn btn-outline w-full p-0">
    <box-icon type="solid" name="eraser" class="fill-current"></box-icon>
  </button>
</div>

<dialog x-ref="clear" class="modal">
  <div class="modal-box">
    <div>Хотите отменить выбор блюд и продуктов?</div>
    <div class="modal-action">
      <form method="dialog" class="flex gap-2">
        <button @click="clearAll" class="btn">Да</button>
        <button class="btn">Нет</button>
      </form>
    </div>
  </div>
</dialog>
